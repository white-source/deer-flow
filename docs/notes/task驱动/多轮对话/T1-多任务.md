
用户可以持续发消息，消息进入任务队列；Agent 在后台运行多个 Worker，而不是今天这种“一个问题锁死整个会话直到结束”的模式。
如果要实现这个需求deerFlow 要做哪些修改才能支持

```text
分析 DeerFlow 当前的会话/run 并发模型，并梳理实现「消息队列 + 多 Worker」所需的改动。

需求在「同线程排队」与「同线程多 Worker 并行」之间差异很大，先确认目标语义



```



```text
同一会话（同一 thread_id）里，多条用户消息期望如何执行？

A: 排队串行，用户可连续发消息入队，后台按顺序一条一条的跑agent
B: 同线程并行： 多消息同时跑多个agent Worker ，各自产出回复后再合并到回话
C: 都要，默认排队，可选开启并行worker 例如（Ultra模式/子任务模式）

```
